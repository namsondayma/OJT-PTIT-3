
const invoicesSection = document.getElementById('invoices');
const productsSection = document.getElementById('products');
const accountsSection = document.getElementById('accounts');

// Code để thực hiện các chức năng quản lý
// Ví dụ: Lấy dữ liệu hóa đơn từ máy chủ và hiển thị nó trên trang
function getInvoices() {
    // Gửi yêu cầu AJAX để lấy dữ liệu hóa đơn từ máy chủ
    // Xử lý dữ liệu nhận được và hiển thị trên trang
}

// Gọi hàm khi trang web được tải
window.onload = function() {
    getInvoices();
    // Gọi các hàm tương tự cho sản phẩm và tài khoản
};